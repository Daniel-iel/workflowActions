name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install
      working-directory: ./web
      run: npm install
    - name: Build
      working-directory: ./web
      run: |
        npm run test


